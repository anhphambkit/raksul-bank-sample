# Raksul-bank

Nuxt 4 SSR application foundation with strict TypeScript and Nuxt UI. Banking features are pending.

## Run locally

Use Node.js 24.14.0 and npm 11.

```bash
npm ci
npm run dev
```

Run `npm run typecheck`, `npm run lint`, `npm run test -- --run`, and `npm run build` to verify changes.
